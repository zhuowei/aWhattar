{
	"manifest_version": 2,
	"name": "aWhattar",
	"version": "1.0",
	"default_locale": "en",
	"description": "Turns all Twitter avatars into abstract stripes. See how many you still recognize! This addon runs once a second, so some avatars might not be caught exactly when they load.",
	"content_scripts": [
		{
			"matches": ["https://twitter.com/*"],
			"js": ["awhattar.js"]
		}
	]
}